<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noteverso.core.dao.AttachmentRelationMapper">
    <insert id="batchInsert">
        INSERT INTO noteverso_attachment_map (attachment_id, object_id, creator, updater, added_at, updated_at)
        VALUES
            <foreach collection="attachmentRelations" item="item" separator=",">
                (#{item.attachmentId}, #{item.objectId}, #{item.creator}, #{item.updater}, #{item.addedAt}, #{item.updatedAt})
            </foreach>
    </insert>

    <select id="getAttachmentCountByObjectIds" resultType="com.noteverso.core.model.dto.AttachmentCount">
        SELECT object_id AS object_id, COUNT(*) AS attachment_count FROM noteverso_attachment_map
        <where>
            <if test="objectIds != null and objectIds.size() > 0">
                object_id IN
                <foreach collection="objectIds" item="objectId" open="(" close=")" separator=",">
                    #{objectId}
                </foreach>
            </if>
            <if test="objectIds == null or objectIds.size() == 0">
                AND 1 = 0
            </if>
            AND creator = #{userId}
        </where>
        GROUP BY object_id;
    </select>
</mapper>
