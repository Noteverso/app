<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noteverso.core.dao.AttachmentMapper">
    <insert id="batchInsert">
        INSERT INTO noteverso_attachment(name, type, url, resource_type, size, note_id, project_id, comment_id, creator, updater, added_at, updated_at)
        VALUES
            <foreach collection="files" item="file" separator=",">
                (#{file.name}, #{file.type}, #{file.url}, #{file.resourceType}, #{file.size},
                #{file.noteId}, #{file.projectId}, #{file.commentId}, #{file.creator}, #{file.updater}, #{file.addedAt}, #{file.updatedAt})
            </foreach>
    </insert>
</mapper>
