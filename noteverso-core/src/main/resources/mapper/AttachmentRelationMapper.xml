<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noteverso.core.dao.AttachmentRelationMapper">
    <insert id="batchInsert">
        INSERT INTO noteverso_attachment_map (attachment_id, note_id, project_id, comment_id, creator, updater, added_at, updated_at)
        VALUES
            <foreach collection="attachmentRelations" item="item" separator=",">
                (#{item.attachmentId}, #{item.noteId}, #{item.projectId}, #{item.commentId}, #{item.creator}, #{item.updater}, #{item.addedAt}, #{item.updatedAt})
            </foreach>
    </insert>
</mapper>
