<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noteverso.core.dao.ProjectMapper">
    <select id="selectByProjectId" resultType="com.noteverso.core.model.Project">
        SELECT * FROM noteverso_project
        WHERE project_id = #{projectId} AND creator = #{userId}
    </select>

    <select id="getProjects" resultType="com.noteverso.core.model.Project">
        SELECT name, project_id, color FROM noteverso_project
        WHERE creator = #{userId}
        <if test="request.name">
            AND LOWER(name) LIKE LOWER(CONCAT('%', TRIM('${request.name}'), '%'))
        </if>
        <if test="request.projectIds != null and request.projectIds.size() > 0">
            AND project_id IN
            <foreach collection="request.projectIds" item="projectId" open="(" close=")" separator=",">
                #{projectId}
            </foreach>
        </if>
        ORDER BY added_at DESC
    </select>
</mapper>
