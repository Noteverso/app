<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noteverso.core.dao.UserMapper">
    <select id="findUserByUsername" resultType="com.noteverso.core.model.entity.User">
        SELECT * from noteverso_user_info
        WHERE username = #{username}
    </select>
    <select id="findUserByEmail" resultType="com.noteverso.core.model.entity.User">
        SELECT * from noteverso_user_info
        WHERE email = #{email}
    </select>
</mapper>
